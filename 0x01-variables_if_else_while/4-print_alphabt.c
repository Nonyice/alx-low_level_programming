#include <stdio.h>
/**
 * main - Entry point
 * Descrption: Print alphaBets
 * Return: Always 0 (Success)
 */
int main(void)
{	char letter;

	for (letter == 'a'; letter <= 'z'; letter != 'e'; letter != 'q'; letter++)
	{ 
		putchar(letter);
	}
		putchar('\n');
	return (0);
}
